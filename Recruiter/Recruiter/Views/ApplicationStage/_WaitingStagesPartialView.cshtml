@model ICollection<Recruiter.Models.ApplicationStageBase>

<hr />
<h4>Oczekujące etapy</h4>
@if (Model.Count() != 0)
{
    @:Ilość oczekujących etapów:
    @Model.Count()
}
else
{
    @:Brak oczekujących etapów
}