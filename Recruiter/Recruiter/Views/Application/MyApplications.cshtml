@*@using Recruiter.Models.ApplicationViewModels
@model IEnumerable<MyApplicationsViewModel>

@{
    ViewData["Title"] = "My Applications";
}

<h2>My Applications</h2>

@if (!Model.Any())
{
    <tr><td colspan="3" class="text-center">No applications</td></tr>
}
else
{
    <table class="table table-condensed table-bordered" header header-content="My applications">
        <tr>
            <th>Name</th>
            <th>Apply time</th>
        </tr>

        @foreach (MyApplicationsViewModel application in Model)
        {
            <tr>
                <td>
                    <a asp-action="MyApplicationDetails" asp-route-id="@application.Id" asp-route-returnurl="@Context.Request.Path">@application.JobPosition.Name</a>
                </td>
                <td>
                    @Html.DisplayFor(model => application.CreatedAt)
                </td>
            </tr>
        }
    </table>
}*@


@*@if (!Model.Any())
{
    <tr><td colspan="3" class="text-center">No applications</td></tr>
}
else
{
    <table class="table table-condensed table-bordered" header header-content="my application">
        <tr>
            <th>Name</th>
            <th>Akcje</th>
        </tr>

        @foreach (MyApplicationsViewModel application in Model)
        {
            <tr>
                <td>@application.JobPosition.Name</td>
                <td>
                    <a class="btn btn-sm btn-default" asp-action="MyApplicationDetails" asp-route-id="@application.Id" asp-route-returnurl="@Context.Request.Path">Details</a>
                    <form asp-action="Delete" asp-route-id="@jobPosition.Id" method="post">
                            <a class="btn btn-sm btn-default" asp-action="Details" asp-route-id="@jobPosition.Id" asp-route-returnurl="@Context.Request.Path">Details</a>
                            <button type="submit" onclick="return confirm('Do you really want to delete this job position?');" class="btn btn-sm btn-danger">
                                Delete
                            </button>
                        </form>
                </td>
            </tr>
        }
    </table>
}*@