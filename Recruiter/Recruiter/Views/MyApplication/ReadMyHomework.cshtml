@using Recruiter.Models
@model Homework

@{
    ViewData["Title"] = "My Homework";
}

<h2>My Homework</h2>

<div asp-validation-summary="All" class="text-danger"></div>

<div>
    <hr />
    <dl class="dl-horizontal">
        @*<dt>
            @Html.DisplayNameFor(model => model.Duration)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Duration)
        </dd>*@
        <dt>
            @Html.DisplayNameFor(model => model.StartTime)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StartTime)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.EndTime)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.EndTime)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>

<div>
    <form asp-action="SendMyHomework" asp-route-id="@Model.Id" method="post">
        <input type="hidden" name="Id" value="@Model.Id" />

        <div class="form-group">
            <label asp-for="Url" class="col-md-1 control-label"></label>
            <div class="col-md-10">
                <input asp-for="Url" class="form-control" />
                <span asp-validation-for="Url" class="text-danger"></span>
            </div>
        </div>

        <br />
        <br />
        
        @*<div class="form-group">
            <input type="submit" onclick="return confirm('Do you really want to send homework?');" class="btn btn-block" value="Create" />
            <a asp-action="Index" class="btn btn-block">Cancel</a>
        </div>*@

        <div class="form-group">
            <button type="submit" onclick="return confirm('Do you really want to send homework?');" class="btn btn-sm btn-block">
                Send homework
            </button>
            <a class="btn btn-sm btn-block" asp-action="MyApplicationDetails" asp-route-id="@Model.ApplicationId">Back</a>
        </div>




        @*<a condition="@ViewData["ReturnUrl"] != null" class="btn btn-sm btn-default" href="@ViewData["ReturnUrl"]">Back</a>
        <a condition="@ViewData["ReturnUrl"] == null" class="btn btn-sm btn-default" asp-action="MyApplications">Back</a>*@
    </form>
</div>
